const originalArray = document.getElementById
const 

function selsort(arr:number[]):number[]{
    let i = 0
    for(let j=i+1;j<arr.length;j++){
    if(arr[j]<arr[smallest]); smallest = j
        }
        [arr[i],arr[smallest]] = [arr[smallest],arr[i]]
    return arr;
    }

console.log(selsort([5,2,1,6]))
//n^2

//what the fuck, homie moves too fast. im slow